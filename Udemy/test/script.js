"use strict";

window.addEventListener("load", () => {

  let x = 11;

  let a = 10;
  let b = 20;
  // let a = true;
  // let b = false;


  if (x > a && x < b) {
    console.log("x ist Grösser als A " + `(${a})` + " UND Kleiner als B " + `(${b})`);
  }
});
