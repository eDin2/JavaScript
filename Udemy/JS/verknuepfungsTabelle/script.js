"use strict";

window.addEventListener("load", () => {
  let tBody = document.getElementById('tab').children[1];
  // damit hohle ich mir den tbody der tabelle
  // der tbody ist das zweite kind der tabelle


});
