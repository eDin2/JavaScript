//jshint esversion:6
"use strict";

/* var eingabe = prompt("Gib deinen Namen ein: ");
var firstLetter = name.charAt(0).toUpperCase();
var restDerEingabe = eingabe.slice(1).toLowerCase();
var name = firstLetter + restDerEingabe;

console.log(name);
 */

let getMilk = function (botel) {
  let cost = botel * 1.3;
  return cost;
};

getMilk(3);

console.log(getMilk);

